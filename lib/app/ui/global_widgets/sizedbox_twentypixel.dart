import 'package:flutter/material.dart';

class SizedBoxTwentyPixels extends StatelessWidget {
  const SizedBoxTwentyPixels({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}